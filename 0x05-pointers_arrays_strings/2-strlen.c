#include "main.h"
#include <string.h>

/**
 *_strlen - This returnn the length of a string
 *@s: This is the pointer of the string to be checked
 *Return: void
 */
int _strlen(char *s)
{
return (strlen(s));
}

