#include "main.h"

/**
 * mul - main multiplication function
 *@a: first argument
 *@b: second argument
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
        return (a * b);
}