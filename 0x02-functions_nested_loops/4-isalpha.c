#include <stdio.h>

int main ()
{
    int letter;

    if ((letter >= 'a' && letter <= 'z') || (letter >= 'A' && letter <= 'Z'))
          return(1);
    else;
          return(0);

    putchar('\n');
}