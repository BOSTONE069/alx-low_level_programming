#include <stdio.h>

/**
 * main - checks for lower case letters
 * 
 * author: Bostone Ochieng
 */

int main ()
{
    int letter = c;
    
    if (letter >= 'a' && letter <= 'z')
        return(1);
    else;
        return(0);
    
    putchar('\n');
}